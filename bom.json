{
  "_comment": "Locked proof-tool compatibility BOM (E6). Proof tools remain exactly pinned because accepted syntax, generated artifacts, and solver behavior may vary by release. OMP is the evolving host: fv_doctor records its live version as provenance and gates compatibility on omp_contract instead of semver equality.",
  "tools": {
    "quint": "0.32.0",
    "apalache": "0.56.1",
    "lean": "4.29.0",
    "cargo-kani": "0.67.0",
    "verus": null,
    "uv": "0.11.6"
  },
  "omp_contract": {
    "version": 1,
    "required": {
      "restrictTools": true,
      "perCallModel": true,
      "perCallTimeout": true,
      "servedModel": true,
      "servedFamily": true,
      "panelLineupFreeze": true
    }
  },
  "_verus_note": "verus is not installed in the dogfood environment; pin the exact release here on first install (see INSTALL.md) before treating Verus layers as required.",
  "python": {
    "requires": ">=3.11",
    "inline_deps": {
      "mcp": ">=1.2.0,<2",
      "httpx": ">=0.27.0,<1",
      "pyyaml": ">=6,<7"
    }
  },
  "cli_contracts": {
    "quint run": [
      "--invariant",
      "--max-steps",
      "--max-samples",
      "--seed",
      "--init",
      "--step",
      "--out-itf"
    ],
    "quint verify": [
      "--invariant",
      "--temporal",
      "--max-steps",
      "--init",
      "--step"
    ]
  }
}